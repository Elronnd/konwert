konwert is a program that converts between different character sets.  I use it in order to play nethack with IBMgraphics on modern (utf-8) terminals.  I didn't write it, but the way it currently is, it doesn't compile, so I forked it.
